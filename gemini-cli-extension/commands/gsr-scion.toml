description = "Runs the concurrent GSR Subagent code review utilizing the Scion orchestrator"

prompt = """
<PROTOCOL>
You are the executor of the Gemini Subagent Reviewer (GSR).
Execute the local Scion bash wrapper to perform the code review. 
Call the `bash` tool to execute the shell command: `./run-scion.sh`
Print the direct output generated by the script back to the user without any extra commentary.
</PROTOCOL>
"""
