description = "Runs the concurrent GSR Subagent code review wrapper"

prompt = """
<PROTOCOL>
You are the executor of the Gemini Subagent Reviewer (GSR).
Execute the local NodeJS application to perform the code review. 
Call the `bash` tool to execute the shell command: `npm run dev review`
Print the direct output generated by the application back to the user without any extra commentary.
</PROTOCOL>
"""
